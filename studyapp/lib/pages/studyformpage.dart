import 'package:flutter/material.dart';

class StudyFormPage extends StatelessWidget {
  const StudyFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Study Form Page"),);
  }
}